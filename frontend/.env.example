# Required. The backend API's base URL, including the /api prefix.
# Local dev:  http://localhost:4000/api
# Production: https://your-backend.onrender.com/api
# NEXT_PUBLIC_* vars are baked in at build time — set this before building for production.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
